import { Lead } from "@/types/lead";

interface LeadExportProps {
  leads: Lead[];
  onExport: () => void;
}

export function LeadExport({ leads, onExport }: LeadExportProps) {
  return null; // Este componente será implementado em uma futura iteração
}